//Declara dos variables: **`numero1`** y **`numero2`**, una como cadena y otra como número.
//  Utiliza la función **`parseInt()`** para convertir la cadena en un número, y luego suma ambas variables.

function ejecutarEjercicio10() {
    let numero1 ="7";
let numero2 = 7;
let numeroN= parseInt(numero1); //declaro una nueva variable, llamo la funcion, dentro de los parentesis coloco la variable que quiero convertir
console.log(numeroN + numero2);
}